import { PropsWithChildren } from "react"

type Props = {
    name:string,
    params:string
}

export const Community = (props:PropsWithChildren<Props>) => {

    return(
        <>
        </>
    )
}